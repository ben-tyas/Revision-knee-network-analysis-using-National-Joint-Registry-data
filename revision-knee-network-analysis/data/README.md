# Data directory

Patient-level NJR data are not distributed with this repository.

Authorised users should place the source extract at:

`raw/Raw data.dta`

`derived/` is used by the analysis workflow for generated datasets and is excluded from version control.
